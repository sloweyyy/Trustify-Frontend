import React from 'react';

/**
 * Payment route component that allows access to payment-related pages
 * regardless of authentication status or user role
 */
const PaymentRoute = ({ children }) => {
  return children;
};

export default PaymentRoute;
